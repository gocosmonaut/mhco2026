parameters:
  level: 1
  paths:
    - .
  ignoreErrors:
    # TypedRegisterForm must extend the user module's register form, which is
    # marked @internal. There is no public base form to extend instead.
    -
      message: '#extends @internal class#'
      path: src/TypedRegisterForm.php
